.DS_Store
node_modules/
*.log
.env
.env.*
dist/
build/
__pycache__/
*.pyc
.pytest_cache/

# pi session exports (/export from the TUI) — personal, large, not repo content
*.jsonl

# audit loop kill-switch — never commit this, it would permanently disable
# the loop for anyone who clones the repo (see .pi/audit/README.md)
.pi/audit/STOP
